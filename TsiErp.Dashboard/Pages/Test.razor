@page "/test"
@using TsiErp.Dashboard.Services
@inject IstasyonOEEService IstasyonOEEService



@code {

    DateTime date1;
    DateTime date2;
    int groupID;

    protected override void OnInitialized()
    {

        //var list = IstasyonService.GetStationGeneralAnalyies(date1,date2);
        //var list = IstasyonDetayService.GetStationDetailedHaltAnalysis(makineID,date1,date2);
        //var list = IstasyonDetayService.GetStationDetailedProductAnalysis(makineID,date1,date2);
        //var list = IstasyonDetayService.GetStationDetailedEmployeeAnalysis(makineID,date1,date2);
        //var list = StokService.GetProductGroupsAnalysis(date1,date2);
        //var list = StokDetayService.GetProductScrapAnalysis(groupID,date1,date2);
        //var list = PersonelService.GetEmployeeGeneralAnalysis(date1,date2);
        var list = IstasyonOEEService.GetStationOEEAnalysis(date1,date2);
    }
}
